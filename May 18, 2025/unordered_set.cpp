#include<iostream>
#include<unordered_set>
using namespace std;

int main(){
    // Random order - Not sorted
    // Unique
    
    unordered_set<int> un_set;
    
    // most of the cases time complexity is O(1) - erase, insert
    // worst case - O(n)
    
    return 0;
}
